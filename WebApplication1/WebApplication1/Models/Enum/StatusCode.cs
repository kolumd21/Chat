namespace WebApplication1.Models.Enum
{
    public enum StatusCode
    {
        AccountIsHasAlready = 1,
        AccountDontRegistred = 0,

        OK = 200,
        Incorrect = 600,
        InternalServerError = 500

    }
}
